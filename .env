# Shared by every mode. The API URL deliberately lives in .env.development and
# .env.production instead, because keeping it here means one stale edit silently
# points `npm run dev` at the live server — which is exactly what happened, and it
# looks like a caching bug rather than a wrong-host bug.
VITE_VAPID_PUBLIC_KEY=BOqH_KMno7pieIb_W4K-TQ3jjZlswTHqYpgvuBc8EB6SrPrPQ-0Pi-YK5Et34kus7hzL7YS_hmA7n2AdC37OQI4
